export interface ReduxInput {
  name: string;
  input: string;
};

export interface SelectMatchState {
  team: string;
  match: string;
  event: string;
};

export interface CommentsState { 
    defence: string;
    penalties: string;
    other: string;
}
